{
  "name": "gchat-markdown-exporter",
  "version": "0.1.0",
  "private": true,
  "description": "A local-only Chrome extension for exporting visible Google Chat messages to Markdown.",
  "scripts": {
    "test": "node --test"
  }
}
